<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.personal.shopping.domain.mapper.CategoryMapper">

  <select id="searchCategory" resultType="Category">
    select category_id, parent_category_id, category_name, created_at, updated_at, delete_flag, deleted_at
      from category
  </select>

</mapper>