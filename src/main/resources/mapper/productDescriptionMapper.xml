<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.personal.shopping.domain.mapper.ProductDescriptionMapper">

  <!-- 상품 아이디 기준 상세 설명 조회 -->
  <select id="findByProductId" parameterType="Long" resultType="ProductDescription">
    select product_description_id
         , product_id
         , contents_type
         , contents_value,sequence
      from product_description
     where product_id = #{productId}
  order by sequence
  </select>
</mapper>